# def DNA_strand(dna):
# code here

# Read the input of the user. The function provides the input.

# Use a key array to replace each letter

# If A then replace with T
# If T then replace with A
# If C then replace with G
# if G then replace with C

# Cannot modify a string directly.
dna = "ATTAG"


# Add letters to the end of the string?

if (dna[0] == 'A'):
    dna = dna + 'T'

print(dna[3:])
